import react from 'react'
import './styles.css'

export default function PhaseLink(props) {
    return (
        <div className="containerPhaseLink">
            <h1>{props.name}</h1>
        </div>
    )
}